跨组建传递信息 
创建  const ThemContext = React.createContext('skyblue'); 
provider 提供   Consumer 一个函数接受  useContext 接受

prop-types  鉴定类型  npm install --save prop-types

import PropTypes from 'prop-types';

类组件
 static contextTypes = {
        color: PropTypes.string,
    }
//props 默认类型
 static propTypes = {
        value: PropTypes.string,
    }
//props 默认值
  static defaultProps = {
        num:0
    }

 Grandson.propTypes = {
    num: PropTypes.number,
}
Grandson.defaultProps = {
    num:0
}
//函数组件
Son.PropTypes={
    name:PropTypes.string
}
// 申明静态变量、contextType 将 context 直接赋值于 contextType
  static contextType = ThemeContext

   const theme = this.context